info:
  name: Cancel Form Session
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/forms/{{sessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 OK
    request:
      url: ""
      method: DELETE
    response:
      statusText: "200"
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: '{"cancelled": true}'

docs: Cancel a form session.
