info:
  name: Get Form Session
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/forms/{{sessionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get form session information.
