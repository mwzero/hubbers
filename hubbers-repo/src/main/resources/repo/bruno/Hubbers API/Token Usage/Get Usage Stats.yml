info:
  name: Get Usage Stats
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/usage"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get LLM token usage statistics grouped by provider.
