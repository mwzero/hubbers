info:
  name: List Pipelines
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/pipelines"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all available pipeline manifests.
