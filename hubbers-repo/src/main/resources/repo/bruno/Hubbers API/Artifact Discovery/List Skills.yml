info:
  name: List Skills
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/skills"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all available skill manifests.
