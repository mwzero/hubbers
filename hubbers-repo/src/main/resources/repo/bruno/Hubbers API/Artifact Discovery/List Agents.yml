info:
  name: List Agents
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/agents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all available agent manifests.
