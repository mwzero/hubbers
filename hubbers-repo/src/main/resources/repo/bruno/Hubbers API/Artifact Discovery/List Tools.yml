info:
  name: List Tools
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/tools"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all available tool manifests.
