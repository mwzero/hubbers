info:
  name: List Tools (v1)
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/v1/tools"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all Hubbers artifacts (agents, tools, pipelines, skills) as OpenAI function tool definitions.
