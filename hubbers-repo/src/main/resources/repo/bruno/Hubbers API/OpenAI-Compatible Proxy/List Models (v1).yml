info:
  name: List Models (v1)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/v1/models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List available models in OpenAI-compatible format. Returns a virtual model with Hubbers tool capabilities.
