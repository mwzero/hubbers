info:
  name: Get Execution Output
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/executions/{{executionId}}/output"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get the output from an execution.
