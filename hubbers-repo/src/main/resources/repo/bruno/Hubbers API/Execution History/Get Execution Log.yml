info:
  name: Get Execution Log
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/executions/{{executionId}}/log"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get execution logs (plain text).
