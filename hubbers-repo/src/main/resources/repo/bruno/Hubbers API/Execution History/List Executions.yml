info:
  name: List Executions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/executions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all execution history entries.
