info:
  name: Get Execution Input
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/executions/{{executionId}}/input"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get the input that was provided to an execution.
