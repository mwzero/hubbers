info:
  name: Get Execution Metadata
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/executions/{{executionId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get metadata for a specific execution.
