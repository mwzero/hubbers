info:
  name: Get Settings
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/settings"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Retrieve the current application configuration (AppConfig).
