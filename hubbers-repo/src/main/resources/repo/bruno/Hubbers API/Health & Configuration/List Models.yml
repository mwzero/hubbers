info:
  name: List Models
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/models"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List available LLM models from all configured providers (Ollama, OpenAI, Anthropic, etc.).
