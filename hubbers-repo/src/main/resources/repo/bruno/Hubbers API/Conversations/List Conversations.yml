info:
  name: List Conversations
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/conversations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: List all stored conversations.
