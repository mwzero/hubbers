info:
  name: Get Conversation Messages
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/conversations/{{conversationId}}/messages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get all messages in a conversation.
